import React from "react"

const CustomMatcher = () => {
    return (
        <form aria-label="form">
            <button>Cancel</button>
            <button>Apply</button>
        </form>
    )
}

export default CustomMatcher
